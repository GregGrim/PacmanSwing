package model.items;

import model.GameBoard;

import java.awt.*;
// wall of maze
public class Block extends Item {
    public Block(Point p, GameBoard gameBoard) {
        super(p, gameBoard);
    }
}
